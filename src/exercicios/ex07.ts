let livro: {
    titulo: string;
    autor: string;
    paginas: number;
};

// Atribuindo um objeto literal com os valores
livro = {
    titulo: 'Senhor dos Anéis',
    autor: 'J. R. R. Tolkien',
    paginas: 576,
};

// Imprimindo as informações
console.log(`Título: ${livro.titulo}\nAutor: ${livro.autor}\nPáginas: ${livro.paginas}`);
